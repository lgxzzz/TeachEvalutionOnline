<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teaching.evaluation">
    <permission android:name="android.permission.INTERNET"></permission>
    <application
        android:name=".TeachingEvaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:theme="@android:style/Theme.Black.NoTitleBar"
        >
        <activity android:name=".GuidActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".ChooseRoleActivity"></activity>
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".test.DBTestAcitivity"></activity>
        <activity android:name=".EvaluationActivity"></activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".DiaryActivity"></activity>
    </application>

</manifest>